# Project locations (merged)

**Canonical repo — use this folder for all work:**

`/Users/philipmacleod/Applications`

Also available as:

- `~/hibs-betting-app` → symlink to `Applications`
- Open in Cursor: **File → Open Folder → Applications** (not `betting-app`)

## Merged from `~/HibsBetting`

- `src/hibs_predictor/m5_optimization.py` — Apple Silicon + free-tier API tuning
- `check_system.py` — pre-flight checks (`python check_system.py`)
- `M5_AND_FREE_API_OPTIMIZATION.md`, `MACOS_QUICKSTART.md`, `OPTIMIZATION_COMPLETE.md`

`HibsBetting` is an older snapshot; do not edit it for new features.

## Other folders (ignore for hibs-bet)

| Path | Notes |
|------|--------|
| `~/betting-app` | Empty stub + old venv; not the app |
| `~/hibs_betting_predictor` | Different backend/frontend layout |
| `~/HibsBetting` | Superseded by Applications |
